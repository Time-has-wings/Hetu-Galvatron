profiler_hardware:
  num_nodes: 1
  num_gpus_per_node: 8
  master_addr: "$MASTER_ADDR"
  master_port: "$MASTER_PORT"
  node_rank: "$RANK"
  max_tp_size: 8
  envs:
    - "NCCL_DEBUG=WARN"
    - "NCCL_IB_DISABLE=0"
    - "NCCL_IB_HCA=mlx5_2,mlx5_5"
  max_pp_deg: 8
  overlap_time_multiply: 4
